name: CI Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install black isort ruff pytest pyyaml

    - name: Run Black
      run: black --check train.py

    - name: Run isort
      run: isort --check-only train.py

    - name: Run Ruff
      run: ruff check train.py

    - name: Run tests
      run: pytest tests/ -v
